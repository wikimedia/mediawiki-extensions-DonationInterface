{{# fname_required }}{{! if we don't need the fname, hide the whole name cell }}
						<tr>
							<td>
								<input class="halfwidth" name="fname" value="{{ fname }}" type="text" placeholder="{{ l10n "donate_interface-donor-fname" }}" id="fname" required autocomplete="billing given-name cc-given-name" maxlength="64"><input class="halfwidth" name="lname" value="{{ lname }}" type="text" placeholder="{{ l10n "donate_interface-donor-lname" }}" id="lname" required autocomplete="billing family-name cc-family-name" maxlength="64">
							</td>
						</tr>
                        {{{ fieldError "fname" }}}
                        {{{ fieldError "lname" }}}
{{/ fname_required }}
{{# address_required }}
{{# street_address_required }}
						<tr>
							<td>
								<input class="fullwidth" name="street_address" value="{{ street_address }}" type="text" title="{{ l10n "donate_interface-donor-street_address" }}" placeholder="{{ l10n "donate_interface-donor-street_address" }}" id="street_address" required autocomplete="billing address-line1" maxlength="96">
							</td>
						</tr>
                        {{{ fieldError "street_address" }}}
{{/ street_address_required }}
						<tr>
							<td>
{{# city_required }}
								<input class="{{ address_css_class }}" name="city" value="{{ city }}" type="text" placeholder="{{ l10n "donate_interface-donor-city" }}" id="city" required autocomplete="billing address-level2" maxlength="64">
{{/ city_required }}
{{> state_dropdown }}{{! only renders if required }}
{{# postal_code_required }}
								<input class="{{ address_css_class }}" name="postal_code" value="{{ postal_code }}" type="text" placeholder="{{ l10n "donate_interface-donor-postal_code" }}" id="postal_code" required autocomplete="billing postal-code" maxlength="64">
{{/ postal_code_required }}
							</td>
						</tr>

    {{# city_required }}
                        {{{ fieldError "city" }}}
    {{/ city_required }}
    {{# state_required }}
                        {{{ fieldError "state" }}}
    {{/ state_required }}
    {{# postal_code_required }}
                        {{{ fieldError "postal_code" }}}
    {{/ postal_code_required }}
{{/ address_required }}
{{# fiscal_number_required }}
						<tr>
							<td>{{! TODO: Explore the new pattern attribute for browser validation while allowing the donor to enter whatever punctuated format they're used to }}
								<input class="fullwidth" name="fiscal_number" value="{{ fiscal_number }}" type="text" title="{{ l10n "donate_interface-donor-fiscal_number" }}" placeholder="{{ l10n "donate_interface-donor-fiscal_number" }}" id="fiscal_number" required >
							</td>
						</tr>
                        {{{ fieldError "fiscal_number" }}}
{{/ fiscal_number_required }}
{{# email_required }}
						<tr>
							<td>
								<input class="fullwidth" name="email" value="{{ email }}" type="email" title="{{ l10n "donate_interface-donor-email" }}" placeholder="{{ l10n "donate_interface-donor-email" }}" id="email" required autocomplete="email" maxlength="254">
							</td>
						</tr>
                        <tr id="emailSuggestion" >
                            <td>
                                <span></span><div class="close-button">×</div>
                            </td>
                        </tr>
                        {{{ fieldError "email" }}}
{{/ email_required }}
